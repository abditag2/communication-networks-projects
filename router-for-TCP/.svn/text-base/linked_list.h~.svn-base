
#include<stdio.h>
#include<stdlib.h>
typedef struct Node 
{

	char addr[20] ;
	char localhost[20];
	char udpport[20]; 
	int cost; 
        struct Node *next;
}node;

node* new_node()
void insert(node *pointer, node* )
